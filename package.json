{
  "name": "ai-news",
  "private": true,
  "scripts": {
    "build": "npm run build:search",
    "build:search": "pagefind --site docs --output-subdir pagefind"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}
